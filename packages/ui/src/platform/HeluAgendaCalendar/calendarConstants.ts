/** Fixed width for each day column in the horizontal calendar grid. */
export const DAY_COLUMN_WIDTH = 112;

/** Days loaded before the anchor when the calendar mounts. */
export const INITIAL_PAST_DAYS = 7;

/** Days loaded after the anchor when the calendar mounts. */
export const INITIAL_FUTURE_DAYS = 28;

/** Days appended when the user scrolls near either edge. */
export const EXTEND_DAYS = 14;

/** How many day-columns from the edge trigger loading more days. */
export const EXTEND_EDGE_THRESHOLD = 4;
