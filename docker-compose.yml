services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      target: web-runner
      args:
        - APP_NAME=@healthguard/web
    container_name: healthguard_frontend_web
    tty: true
    stdin_open: true
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    networks:
      - healthguard_network

  mobile:
    build:
      context: .
      dockerfile: Dockerfile
      target: mobile-runner
      args:
        - APP_NAME=@healthguard/mobile
    container_name: healthguard_frontend_mobile
    working_dir: /app/apps/mobile
    tty: true
    stdin_open: true
    # npx expo start --host lan asegura que el QR sea escaneable por tu celular en la misma red.
    command: npx expo start --port 8081 --host lan
    ports:
      - "8081:8081"
      - "19000:19000"
      - "19001:19001"
      - "19002:19002"
    environment:
      - NODE_ENV=development
      - EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
      # He detectado que tu IP local es 192.168.80.20
      - REACT_NATIVE_PACKAGER_HOSTNAME=192.168.80.20 
    volumes:
      - .:/app
      - /app/apps/mobile/node_modules
      - /app/node_modules

    networks:
      - healthguard_network

networks:
  healthguard_network:
    external: false
